@extends('layouts.frontend')
@section('content')
<div id="root" class="min-h-screen bg-wt-offwhite"><header class="fixed top-0 left-0 right-0 z-40">@include('frontend.partials.announcement')@include('frontend.partials.header')</header><main class="pt-32"><section class="relative min-h-[28rem] flex items-end overflow-hidden bg-wt-oxblood">@if($image)<img class="absolute inset-0 w-full h-full object-cover opacity-55" src="{{ $image }}" alt="{{ $collection->currentDraftRevision->title }}">@endif<div class="relative z-10 max-w-screen-xl w-full mx-auto px-6 lg:px-12 py-16"><p class="font-label text-xs tracking-[0.3em] uppercase text-wt-gold">The Collection</p><h1 class="font-heading text-5xl lg:text-7xl text-wt-cream mt-3">{{ $collection->currentDraftRevision->title }}</h1><p class="font-body text-wt-cream/80 max-w-2xl mt-5">{{ $collection->currentDraftRevision->short_description }}</p></div></section><section class="max-w-screen-xl mx-auto px-6 lg:px-12 py-16"><div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">@forelse($products as $product)<a class="group" href="{{ url('/products/'.$product['slug']) }}">@if($product['image'])<div class="aspect-[4/5] overflow-hidden bg-wt-cream"><img class="w-full h-full object-cover object-top transition-transform duration-700 group-hover:scale-105" src="{{ $product['image']['url'] }}" alt="{{ $product['image']['alt'] }}"></div>@endif<h2 class="font-heading text-2xl text-wt-oxblood mt-4">{{ $product['title'] }}</h2><p class="font-label text-xs tracking-widest uppercase mt-2">{{ $product['price'] }}</p></a>@empty<p class="font-body text-wt-charcoal/70">No Products are currently available in this Collection.</p>@endforelse</div></section></main>@include('frontend.partials.footer')</div>
@endsection
