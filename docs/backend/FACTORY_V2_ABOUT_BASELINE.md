# Factory v2 About Baseline

`william-taylor-factory-v2` inherits the immutable v1 roles, identities, four Site Content resources and 43 Media entries, and adds the governed `about` Page manifest. Commands require explicit `--factory=william-taylor-factory-v2`; v1 remains the default until phase closure. Installation uses existing Page actions to create a draft, submit, approve, publish and designate it. Reinstallation reuses the matching revision; scoped reset restores drift without deleting provider binaries. V2 checksum: `916e3f54b4398ecb7d318a47ffd6cbfd161bec6bb0ddcfab8891b43b50491c3b`.
