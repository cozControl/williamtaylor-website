# Site Content Secure Preview

Preview URLs are authenticated, permission checked, signed and expire after 15 minutes. The response is `private, no-store`, carries `X-Robots-Tag: noindex, nofollow`, and visibly states that public storefront projection is inactive.

Preview renders only validated typed fields. It does not alter public routes, canonical URLs, sitemap state, static templates or public cache.
