# Navigation Management

Primary and footer navigation are independent singleton resources. Links are typed as internal paths or HTTPS external URLs. Stable keys, bounded item counts, depth, labels, children and external-tab behavior are validated. JavaScript, data URLs, protocol-relative URLs, raw HTML, CSS classes and arbitrary components are rejected. Editing supports keyboard reorder plus pointer controls.
