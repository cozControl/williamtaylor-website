# Site Content Publishing

Every resource independently supports draft, submit, changes requested, approval, supersession, immediate designation, scheduling, cancellation and unpublish. Announcements also archive/restore. Exact permissions come from the type registry. Transactions, locks, readiness, fingerprints and append-only transitions protect state. All current types allow self-approval; the policy contract also supports prohibition.
