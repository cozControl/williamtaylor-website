# Public Page Route Registry

`PublicPageRouteRegistry` contains exactly one code-owned entry: route `about`, path `/about`, Page key `about`, type `standard`, template `about`, fallback `frontend.about-static`, and renderer `frontend.about-projected`. Unknown keys throw and no request data can extend the registry. There is no slug route, Page API, alias or redirect.
