# COMMERCE-ORDER-ADMIN-1 completion report

Date: 2026-09-10

**COMMERCE-ORDER-ADMIN-1 IMPLEMENTATION READY FOR GENERAL INSPECTION**

Production Orders are available at **Commerce → Orders**, `/admin/commerce/orders`. Physical visual acceptance remains paused because the single browser connection attempt returned `Browser is not available: iab`. Live Snippe Session authentication acceptance remains pending external credential verification; it does not block Order observability.

## Required implementation record

1. **Status:** Production Orders index and detail implemented, with authorized inspection and a bounded existing-Session payment check. Final verification results are recorded below.
2. **Existing demo Admin:** `Admin\OrderController`, `Domain\Orders\Models\Order`, `orders` and related demo tables own `/admin/orders`, including demonstration creation, transitions, notes and receipts. These are separate from production Checkout models.
3. **Isolation:** New `Admin\CommerceOrderController` binds `Domain\Checkout\Models\Order` and queries only `commerce_orders`. No union, `is_demo` fallback or fixture insertion is used in the production workspace. Existing demo routes/actions/gates remain intact.
4. **Navigation:** The existing Commerce group now exposes **Orders** through `admin.commerce.orders.*`. The former demo destination is explicitly **Demo Orders**, visible only under its existing configured-demo gate. Inventory remains in Catalogue; unrelated navigation was not reorganized.
5. **Permissions:** Reuses registered `orders.view`, together with existing authenticated, verified and `admin.access` middleware. All index/detail/filter/payment-check routes enforce it server-side. Permission metadata now describes access to production PII and existing-Session checks. No role grants or speculative mutation permissions were introduced; existing demo mutation permissions do not supply production mutations.
6. **Index architecture:** Server-rendered Blade and an Eloquent paginated production query, with correlated line count, unit sum and payment-attention existence. No client-side all-history dataset.
7. **KPIs:** Global, unaffected by list filters: Total Orders = all production Orders; Awaiting payment = `pending_confirmation` AND `unpaid`; Paid / confirmed = `confirmed` AND `paid`; Awaiting fulfillment = `paid` AND `unfulfilled`; Needs attention = distinct Orders with at least one Payment carrying a non-null `reconciliation_issue`. Counts intentionally overlap across operational concepts. Five fixed aggregate queries; no value/revenue KPI.
8. **Search:** Case-insensitive parameter-bound matching of business number and immutable customer name/email/phone JSON fields; trimmed input limited to 160 characters. Phone searches also remove spacing, parentheses, periods and hyphens. SQL LIKE wildcard semantics apply. Search is submitted by CSRF-protected POST and retained in the current staff member's session, keeping contact terms out of generated URLs and pagination links.
9. **Filters:** Canonical Order status, unpaid/paid, unfulfilled, placed-from/to and Needs attention. Validated values only; local display-date boundaries convert to UTC comparisons. Clear resets search and filters. No speculative shipping/payment-method filters.
10. **Sorting:** `placed_at DESC`, then `id DESC` for deterministic ties.
11. **Pagination:** Existing Orders convention of 20 per page. Validated non-PII filters persist in pagination; contact search persists in the authorized staff session. Applying filters returns to the first page.
12. **Table:** Business Order number, customer name/email, placed time, explicit units/items, immutable total, separate payment/Order/fulfillment badges and View. Address is confined to detail; no New Order action.
13. **Status presentation:** `OrderStatusPresenter` and one badge partial provide friendly labels, date formatting and semantic tones. Ordinary unpaid/pending states remain neutral; confirmed/paid use success styling, attention uses the existing restrained amber treatment. Payment is never inferred from Order status.
14. **Detail:** Canonical authenticated `/admin/commerce/orders/{order}` uses production model binding. Heading, independent statuses, summary, contact/delivery, item snapshots, payments, inventory and timeline form a read-only workspace.
15. **Immutable snapshots:** Titles, SKU, options, unit amounts, quantities and line totals come exclusively from `commerce_order_lines`. Current catalogue revisions/prices cannot rewrite their presentation.
16. **Contextual links:** Bulk existence checks supply per-line View Product and View Inventory links, separately guarded by `products.view` and `inventory.view`. Snapshot rendering does not dereference a required live relation. Archived Product inspection remains supported; missing identities omit links.
17. **Customer:** Saved name, email and normalized phone appear only inside the authorized workspace. No CRM, public contact projection, PII metadata or new request logging.
18. **Delivery:** Saved recipient, phone, street, city, region and optional postal code. No generic amendment form.
19. **Items:** Historical identity and options with readable SKU, unit price, quantity and line total. Line/item count is distinguished from unit quantity.
20. **Money:** Reuses the established integer-based `CartPresenter::format` TZS display convention. Immutable merchandise subtotal and Order total only; no fabricated tax, shipping or discount rows, no floating-point calculation and no monetary KPI.
21. **Snippe panel:** Sanitized local Payment status, safe Session/payment references, failed-payment reference where recorded, last provider status and available attempt/request/completion/failure/expiry timestamps. Rendering makes no provider requests.
22. **Attempts:** All legitimate attempts displayed oldest first with deterministic ID ties and numbered Current, Successful, Failed or Closed identity. A failed current hosted attempt remains visibly Current with a Failed badge; it is not presented as another Order.
23. **Needs attention:** Prominent Order-level notice and friendly per-attempt support explanations based on bounded existing issue codes. Safe references remain available for provider investigation. Historical unresolved issues remain visible even if another attempt succeeds.
24. **Payment check:** Authorized, throttled, CSRF-protected POST binds Payment to Order and requires an active Snippe attempt with an already-persisted immutable Session reference. It calls existing `StartSnippePayment::refresh`; this guard excludes its Session-creation branch. No new Order or Payment is created. Existing I/O lease, provider backoff and next-check time are respected. Configuration, deferred checks, verification issues and completion have explicit feedback. Unbound/ambiguous attempts remain with existing reconciliation and provider investigation; terminal attempts cannot be checked through this action.
25. **Inventory:** Loads canonical reservations and only `order_issue` movements whose source is this production Order. Each item shows reservation state plus active reserved, actually issued and released units. Consumed reservations and negative issue movements remain distinct evidence; release never appears as a receipt or issue.
26. **Timeline:** Curated read-only events from placement, payment attempt/request/failure/completion, Order confirmation/closure, reservation timestamps and actual issue movements. Oldest first, consistently formatted in `app.timezone`. A request timestamp is labelled requested, never proof of successful Session creation. Expiry forecasts are not fake completed events.
27. **Audit reuse:** Includes up to 50 latest `commerce.payment.needs_attention` audit timestamps for the Order's Payments. No raw audit metadata, actor permissions, request/session information or duplicate audit framework. Full audit remains under its existing permission.
28. **Editing:** No generic production Order edit route or form.
29. **Deletion:** No production Order delete route/action; durable records retained.
30. **Financial override:** No manual mark-paid or confirmation; verified provider evidence remains authoritative.
31. **Cancellation:** Deferred to a later explicit Order Operations phase. A provider cancel client alone is not a complete authorized Admin cancellation service with all required race/stock boundaries.
32. **Fulfillment:** Displays canonical Unfulfilled. No pick, pack, ship, tracking, delivery or courier workflow.
33. **Empty state:** “No customer orders yet.” with storefront-origin explanation; filtered emptiness has a separate message. No demo fallback or fixture-creation button.
34. **Responsive index:** Established labeled-row adaptation preserves identity, customer, total, all three statuses and View. Narrow content uses wrapping and minimum-width-zero layout; the dense nine-column table changes to labeled rows before becoming cramped.
35. **Responsive detail:** Summary, paired snapshot panels, items, payment, inventory and timeline stack at narrow widths. Item rows reuse Inventory's labeled-row behavior; references and addresses wrap.
36. **UI references:** Inspected Products index/edit, Inventory index/detail/styles, Catalogue KPI overview and Campaign form. Reuses `x-admin.layout`, `x-admin.flash`, shared panels/headings/buttons/pagination and Inventory styles/table behavior. New bounded presentation consists of the Order badge partial, five-metric arrangement, snapshot definition lists and curated timeline; no design-system replacement or compiled storefront changes.
37. **Privacy:** Existing Admin authentication/verification and permissions protect every route, including direct binding URLs. Index/detail send private no-store, no-referrer and noindex headers. Contact searches use POST/session; generated URLs carry only IDs or non-PII filters. No full address in index, new PII logging or public data expansion.
38. **Secrets:** Explicit view fields exclude API key, webhook secret, auth headers, checkout URL, Payment return reference, attempt key, Order confirmation/submission bearer keys and arbitrary provider/audit JSON. Existing private fields remain private.
39. **Queries:** Fixed aggregate/index query shape with no per-row Payment, line or reservation loads. Test compares rendered query counts for 20 rows versus one. Detail bulk-loads lines, attempts, reservations, relevant issues and catalogue identity lists, with a limited attention audit query; no per-line database access.
40. **Indexes:** Existing unique business number, placed timestamp, Order status and relation/source indexes preserved. Adds only `commerce_orders_payment_fulfillment_placed` on `(payment_status, fulfillment_status, placed_at)` for paid/unpaid operational filtering/counts. Bounded forward migration applied successfully. JSON contains-search remains a bounded paginated scan; no speculative duplicate, JSON/full-text infrastructure or destructive migration.
41. **Demo evidence:** Creates an actual isolated demo Order through `CreateDemoOrder`, disables demo features and proves production list/detail remain available while demo identity/contact do not appear. Production model binding rejects the demo ID; demo index remains disabled. Navigation follows both permissions and the independent demo gate.
42. **Storefront evidence:** Disposable stock → actual Cart POST → Checkout GET/POST → production Order → authorized Admin. Assertions verify business number, customer, amount, units/items, pending/unpaid/unfulfilled and immutable snapshot presentation after live Product title/SKU/price changes and archiving.
43. **Paid evidence:** Actual hosted fake response plus raw-body signed completion webhook produces confirmed/paid/unfulfilled. Admin shows successful attempt, safe references, consumed reservation, actual two-unit issue and payment/confirmation/issue timeline. Terminal Admin check rejected; no live provider request.
44. **Attention evidence:** Signed wrong-amount fixture remains unpaid/pending with active stock reservation. Index KPI/filter and detail support notice/timeline surface attention without false confirmation.
45. **Expiry evidence:** Admin check uses fake authenticated GET for final expired Session. Order becomes payment-expired/unpaid; reservation releases two units, no stock issue is displayed or created. Tests verify POST CSRF, disabled configuration, retry-delay feedback, unauthorized check, unbound attempt rejection and provider authentication failure feedback.
46. **Focused tests:** Final counts below. Search covers all four fields, pagination/filter retention and constant query count; other cases include empty state, private-field absence, failed/current attempt history and direct authorization.
47. **Scoped checks:** Final results below. No full Laravel suite, full Larastan, complete build, dependency audit, Homepage suite, fidelity matrix or BE-6A audit.
48. **Browser:** One connection attempt returned `Browser is not available: iab`; no retries, alternate control or fabricated screenshots. HTTP/Blade/database evidence is supplied; physical visual acceptance is not claimed.
49. **Live credentials:** LIVE SNIPPE CREDENTIAL VERIFICATION PENDING. Existing Session authentication documentation discrepancy remains an external deployment dependency. Orders inspection works with Snippe disabled.
50. **Integration boundary:** Snippe client, payloads, signing, webhook rules, reconciliation/financial lifecycle and inventory ledger were not redesigned. Admin reuses the existing guarded refresh path for already-bound Sessions.
51. **Phase boundary:** No refunds, returns, fulfillment operations, manual financial overrides, customer accounts, invoices, receipts/emails or WhatsApp work started. No automatic next phase.

## Validation and limits

Final Orders Admin run: **8 tests, 425 assertions passed**. Bounded existing Checkout regression: **1 test, 42 assertions passed**. Bounded existing hosted-payment/signed-completion regression: **1 test, 51 assertions passed**. Total across these scoped runs: **10 tests, 518 assertions passed**.

Scoped Larastan passed with zero errors for the new presenter/controller, production Order relation, navigation, permission metadata and new migration. Changed-file Pint check passed. PHP syntax passed for all eight changed/new PHP files. Final Blade compilation passed. No JavaScript was introduced. `git diff --check` passed; protected compiled assets have no diff.

Tests explicitly require SQLite `:memory:` before rebuilding their disposable database. They do not alter persistent catalogue stock or make live provider calls. Only the bounded forward operational index migration changed the local persistent schema; no MySQL fresh/rollback/re-migration or MySQL concurrency acceptance is claimed.

The existing whole-shilling TZS formatter is reused unchanged. Search contact terms are staff-session state, so they are intentionally not shareable through URLs. Needs-attention history is capped at 50 recent audit events; full payment-attempt history remains visible. Local Session expiry is displayed as provider evidence, not used to infer stock release.

An initial fixture required its immutable Product revision timestamp to be populated. A Windows compiled-view file collision during parallel Blade caching/payment verification was resolved by rerunning payment verification alone; final verification keeps shared view-cache work separate. No application error was suppressed and no analysis baseline was added.

AGENTS.md requires the separate `AUTHORIZE_BE6A1_FULL_AUDIT` token before any full audit. Visual and live-provider acceptance remain separate pending checks.
